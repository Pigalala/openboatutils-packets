import java.time.Duration
import java.time.temporal.ChronoUnit

plugins {
    id 'com.gradleup.nmcp.settings' version '1.4.4'
}

rootProject.name = 'minestom-openboatutils-packets'

nmcpSettings {
    centralPortal {
        username = System.getenv("SONATYPE_USERNAME")
        password = System.getenv("SONATYPE_PASSWORD")
        publishingType = "AUTOMATIC"

        def version = System.getenv("TAG_VERSION")
        publicationName = "minestom-openboatutils-packets:$version"

        validationTimeout = Duration.of(30, ChronoUnit.MINUTES)
        publishingTimeout = Duration.ZERO
    }
}
